.text-center
  = form_for current_user, :html => {:class => "form-signin"} do |f|
    - if current_user.errors.any?
      .form-group
        %h2= "#{current_user.errors.full_messages.count}件のエラーが発生しました。"
        %ul
          - current_user.errors.full_messages.each do |message|
            %li= message

    %h1.h3.mb-3.font-weight-normal プロフィール登録

    .form-group
      = f.label :nickname
      = f.text_field :nickname, autofocus: true, class: 'form-control', placeholder: "例)k-i"

    .form-group
      = f.label :image
      = f.file_field :image, class: 'form-control-file'

    .form-group
      = f.label :prefecture_id, "Prefecture"
      = f.collection_select :prefecture_id, Prefecture.all, :id, :name, {prompt: "prefecture"}, {class: 'form-control'}

    .form-group
      = f.label :introduction
      = f.text_area :introduction, rows: "5", class: 'form-control', placeholder: "例)初めまして。現在チワワ♂(15歳)とチワックス♀(6歳)を飼っています。よろしくお願いします。"

    = f.submit "登録する", class: 'btn btn-lg btn-primary btn-block'

