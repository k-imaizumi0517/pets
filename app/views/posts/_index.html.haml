.col-md-4
  .card.mb-4.shadow-sm
    .card-body
      %h3.card-title
        = posts.title
      %p.card-text
        = truncate(posts.text, length: 160)
      %p
        = link_to "View details &raquo;".html_safe, post_path(posts), class: "btn btn-secondary", role: "button"
