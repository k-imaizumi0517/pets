.col-md-4
  .card.mb-4.shadow-sm
    .card-body
      %h3.card-title
        = posts.title
      %p.card-text
        = truncate(posts.text, length: 130)
      = link_to "View details", "#", class: "btn btn-primary"
