%main{:role => "main"}
  .album.py-5.album__background
    .container
      .row
        .col-md-4
          .card.mb-4.shadow-sm.card__background.card__min-height
            .card-body
              %h3.card-title
                = @post.title
              = simple_format(@post.text, class: "card-text")
